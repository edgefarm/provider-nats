apiVersion: stream.nats.crossplane.io/v1alpha1
kind: Stream
metadata:
  name: main
  annotations:
    crossplane.io/external-name: main
spec:
  forProvider:
    config:
      name: main
      subjects:
        - main.>
      retention: Limits
      storage: File
      maxBytes: 102400
      discard: DiscardOld
  providerConfigRef:
    name: default
