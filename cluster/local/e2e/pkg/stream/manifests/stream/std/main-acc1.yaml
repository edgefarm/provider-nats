apiVersion: stream.nats.crossplane.io/v1alpha1
kind: Stream
metadata:
  name: main-acc1
  annotations:
    crossplane.io/external-name: main-acc1
spec:
  forProvider:
    config:
      name: main-acc1
      subjects:
        - main.>
      retention: Limits
      storage: File
      maxBytes: 102400
      discard: DiscardOld
  providerConfigRef:
    name: acc1
